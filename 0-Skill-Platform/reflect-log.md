# CC 反思日志

> 故障记录 + 定期反思，cc 的成长轨迹。
> 实时记录故障，定期深度反思，持续进化。
>
> **归档规则**：每月初把上月日志移到 `5-Archive/反思/{YYYY-MM}.md`

---

## 故障记录

> 每次遇到值得记录的故障/异常，按时间倒序追加到这里。
> 格式：`### {时间} | {标签}` + 现象/根因/处理/教训

### 2026-03-09 15:35 | 环境差异

**现象**：从技术大神处导入的 gh-trending skill 中，飞书通知路径硬编码了 macOS 绝对路径 `/Users/aster/...`，本机 Windows 环境无法执行
**根因**：skill 来自不同环境（macOS），路径未做跨平台适配
**当时的处理**：替换为相对路径 `.claude/skills/tell-me/send.js`
**教训**：导入外部 skill 时，必须检查所有硬编码路径，尤其是跨平台场景

### 2026-03-09 14:40 | 路径问题

**现象**：heartbeat SKILL.md 依赖 `/reflect` skill，但该 skill 不存在，被错误地移除了
**根因**：首次适配时判断"不存在就删掉"，没考虑到这是需要创建的依赖
**当时的处理**：后续创建了 reflect skill 并恢复依赖
**教训**：依赖声明不存在 ≠ 应该删除。应区分"确实无用"和"待创建"

---

## 反思记录

> 定期反思的产出，由 /reflect 模式二生成，按时间倒序追加。

## [reflect] 反思记录 — 2026-03-09

**扫描范围**：最近 2 条故障记录
**发现的模式**：两条故障共同根因 — 导入外部资源时缺乏系统性检查

**深度思考**：
环境差异（硬编码路径）和路径问题（依赖误删）看似不同，实则同源：导入外部 skill 时没有标准化的适配流程。今天一次性导入 14 个 skill，踩了两个坑，说明"批量导入"是高风险操作。已经手动做了一轮全量检查（13/13 PASS），这个检查过程本身就应该固化为可复用的清单，避免下次再凭经验走。

另一个发现：对"依赖不存在"的处理策略有问题。当前默认是"删掉断链依赖"，但正确做法应该是先判断——是"确实废弃"还是"待创建"。这个判断标准可以加到 skill 导入检查清单中。

**改进行动**：
- [x] 在 auto memory 中记录"导入外部 skill 检查清单"（路径、依赖、脚本、环境适配）→ 2026-03-11 已完成
- [x] 依赖不存在时的策略改为：标记 WARN 并询问，不默认删除 → 记入 MEMORY

**改进去向**：
| 改进 | 执行方式 | 优先级 |
|------|----------|--------|
| skill 导入检查清单 | 更新 MEMORY | 高 |
| 依赖处理策略 | 记入 MEMORY 作为行为规范 | 中 |

---

## [reflect] 反思记录 — 2026-03-11

**扫描范围**：自上次反思（2026-03-09）以来，2天内
**新增故障**：0 条
**Scheduler 健康状况**：全部成功，无异常

**深度思考**：
过去2天系统运行稳定，无新故障记录。上次反思的两项改进行动（skill导入检查清单 + 依赖处理策略）均已落地到 MEMORY，闭环完整。

Scheduler 执行记录：心跳巡逻 2小时/次持续运行，每日早报、CC学习、根目录清理均正常。系统自运转良好。

观察到一个轻微风险：scheduler history 中 "10:00 健康提醒"（2026-03-07）有一条停留在"执行中..."未被标记成功，可能是当时异常退出。但后续任务均正常，说明是偶发的非阻塞性异常。

**改进行动**：
- [ ] scheduler 健康检查时，增加对"卡在执行中..."超过1小时的任务的识别逻辑（低优先级，加入 cc-todo）

**改进去向**：
| 改进 | 执行方式 | 优先级 |
|------|----------|--------|
| scheduler 卡住检测 | 加入 f6 巡逻关注点 | 低 |

---

## 统计

> 各标签出现次数，每次反思时更新。

| 标签 | 次数 | 最近一次 |
|------|------|----------|
| 环境差异 | 1 | 2026-03-09 |
| 路径问题 | 1 | 2026-03-09 |

---
